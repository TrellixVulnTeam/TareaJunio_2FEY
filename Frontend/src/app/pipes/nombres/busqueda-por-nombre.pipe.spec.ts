import { BusquedaPorNombrePipe } from './busqueda-por-nombre.pipe';

describe('BusquedaPorNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedaPorNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
