import { EspecialidadIdPipe } from './especialidad-id.pipe';

describe('EspecialidadIdPipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialidadIdPipe();
    expect(pipe).toBeTruthy();
  });
});
