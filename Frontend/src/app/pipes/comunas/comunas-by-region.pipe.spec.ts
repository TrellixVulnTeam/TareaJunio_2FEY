import { ComunasByRegionPipe } from './comunas-by-region.pipe';

describe('ComunasByRegionPipe', () => {
  it('create an instance', () => {
    const pipe = new ComunasByRegionPipe();
    expect(pipe).toBeTruthy();
  });
});
