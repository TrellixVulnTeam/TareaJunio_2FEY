import { IdToNameComunaPipe } from './id-to-name-comuna.pipe';

describe('IdToNameComunaPipe', () => {
  it('create an instance', () => {
    const pipe = new IdToNameComunaPipe();
    expect(pipe).toBeTruthy();
  });
});
