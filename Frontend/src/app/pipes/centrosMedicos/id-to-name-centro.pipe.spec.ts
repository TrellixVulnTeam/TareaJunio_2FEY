import { IdToNameCentroPipe } from './id-to-name-centro.pipe';

describe('IdToNameCentroPipe', () => {
  it('create an instance', () => {
    const pipe = new IdToNameCentroPipe();
    expect(pipe).toBeTruthy();
  });
});
