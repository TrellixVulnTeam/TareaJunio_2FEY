import { CountCitaPipe } from './count-cita.pipe';

describe('CountCitaPipe', () => {
  it('create an instance', () => {
    const pipe = new CountCitaPipe();
    expect(pipe).toBeTruthy();
  });
});
