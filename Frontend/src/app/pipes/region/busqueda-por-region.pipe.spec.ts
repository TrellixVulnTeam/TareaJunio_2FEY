import { BusquedaPorRegionPipe } from './busqueda-por-region.pipe';

describe('BusquedaPorRegionPipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedaPorRegionPipe();
    expect(pipe).toBeTruthy();
  });
});
