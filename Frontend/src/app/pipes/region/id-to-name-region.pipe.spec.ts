import { IdToNameRegionPipe } from './id-to-name-region.pipe';

describe('IdToNameRegionPipe', () => {
  it('create an instance', () => {
    const pipe = new IdToNameRegionPipe();
    expect(pipe).toBeTruthy();
  });
});
